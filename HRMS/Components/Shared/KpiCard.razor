<div class="card kpi-card border border-base-300 border-l-4 bg-base-100 shadow-none @ResolveAccentClass()">
    <div class="card-body p-5">
        <div class="flex items-start justify-between">
            <div class="min-w-0 flex-1">
                <p class="mb-2 text-xs font-semibold uppercase tracking-wide text-base-content/40">@DisplayLabel</p>
                <p class="text-3xl font-bold leading-none text-base-content">@DisplayValue</p>
                @if (!string.IsNullOrWhiteSpace(DisplaySubtitle))
                {
                    <p class="mt-1.5 text-xs text-base-content/40">@DisplaySubtitle</p>
                }
            </div>
            @if (Icon is not null)
            {
                <div class="ml-3 flex h-10 w-10 shrink-0 items-center justify-center rounded-lg bg-base-200 opacity-50">
                    @Icon
                </div>
            }
        </div>
    </div>
</div>

@code {
    [Parameter]
    public string Title { get; set; } = string.Empty;

    [Parameter]
    public string Value { get; set; } = string.Empty;

    [Parameter]
    public string Subtitle { get; set; } = string.Empty;

    [Parameter]
    public string AccentColor { get; set; } = "#3B5BDB";

    [Parameter]
    public string Label { get; set; } = string.Empty;

    [Parameter]
    public string Sub { get; set; } = string.Empty;

    [Parameter]
    public string Accent { get; set; } = string.Empty;

    [Parameter]
    public RenderFragment? Icon { get; set; }

    private string DisplayLabel => string.IsNullOrWhiteSpace(Label) ? Title : Label;
    private string DisplayValue => Value;
    private string DisplaySubtitle => string.IsNullOrWhiteSpace(Sub) ? Subtitle : Sub;

    private string ResolveAccentClass()
    {
        if (!string.IsNullOrWhiteSpace(Accent))
        {
            return Accent switch
            {
                "success" => "border-l-success",
                "error" => "border-l-error",
                "warning" => "border-l-warning",
                "ghost" => "border-l-base-300",
                _ => "border-l-primary"
            };
        }

        return AccentColor switch
        {
            "#22C55E" => "border-l-success",
            "#EF4444" => "border-l-error",
            "#F59E0B" => "border-l-warning",
            "#94A3B8" => "border-l-base-300",
            _ => "border-l-primary"
        };
    }
}
